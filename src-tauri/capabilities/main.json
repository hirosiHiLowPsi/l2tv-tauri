{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "L2TV main window with only core window and event access.",
  "windows": ["main", "beatoraja-calendar"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default"
  ]
}
